<Window x:Class="NMH_Media_Player.SubtitlesViews.SubresyncWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Subtitle Synchronization" Height="200" Width="400"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <StackPanel VerticalAlignment="Center">
            <TextBlock Text="Adjust subtitle timing:" FontSize="16" Margin="0 0 0 10" />
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                <TextBlock Text="Shift (ms): " VerticalAlignment="Center" />
                <TextBox x:Name="ShiftAmountBox" Width="100" Text="0" Margin="5 0 0 0" />
            </StackPanel>
        </StackPanel>

        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="OK" Width="70" Margin="0 10 10 0" Click="OK_Click" />
            <Button Content="Cancel" Width="70" Margin="0 10 0 0" Click="Cancel_Click" />
        </StackPanel>
    </Grid>
</Window>
